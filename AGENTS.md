# AGENTS.md

이 저장소에서 작업하는 에이전트가 따라야 할 규칙입니다.

## 코드 스타일

- **코드 주석과 JSDoc은 한국어로 작성합니다.** (`//`, `/* */`, `/** */` 모두 해당)
  - 식별자(변수·함수·타입 이름)와 로그·에러 문자열은 기존처럼 영어를 유지합니다.
  - 주석 안의 코드 심볼·API 이름(예: `setVelocity`, `FLOOR_SURFACE_Y`)은 그대로 둡니다.
- 새 코드는 주변 코드의 스타일(주석 밀도, 네이밍, 관용구)에 맞춥니다.

## Commit Message Convention

모든 커밋 메시지는 아래 형식을 사용한다.

```text
태그: 제목

- 변경 내용
- 변경 내용
- 변경 내용
```

### 작성 규칙
- 커밋 메시지는 한국어로 작성한다.
- 태그는 영문 소문자로 작성한다.
- 태그를 대괄호로 감싸지 않는다. `[feat]:`가 아니라 `feat:` 형식으로 작성한다.
- 태그 뒤에 콜론과 공백을 넣는다.
- 제목은 해당 커밋의 핵심 변경 사항을 한 문장으로 작성한다.
- 제목 끝에는 마침표를 붙이지 않는다.
- 제목만 보고도 무엇이 변경되었는지 알 수 있어야 한다.
- 본문은 제목 다음에 빈 줄을 하나 넣은 뒤 `-` 목록으로 작성한다.
- 본문에는 실제로 변경된 동작, 적용 범위, 중요한 구현 사항을 적는다.
- 코드의 클래스명, 함수명, 설정값은 백틱으로 감싼다.
- 확인하지 않은 내용이나 실제 변경되지 않은 내용은 적지 않는다.
- 여러 목적의 변경이 섞여 있다면 가능한 한 커밋을 분리한다.
- 커밋을 생성하기 전에 현재 diff를 확인하고, 실제 변경 사항을 기준으로 메시지를 작성한다.

### 허용 태그

- `feat`: 새로운 기능 추가 또는 기존 기능의 동작 확장
- `fix`: 버그 수정
- `refactor`: 기능 변화 없이 코드 구조 개선
- `perf`: 성능 개선
- `test`: 테스트 코드 추가 또는 수정
- `docs`: 문서 수정
- `style`: 포맷, 공백, 이름 정리 등 동작에 영향이 없는 변경
- `chore`: 설정, 에셋 정리, 단순 유지보수
- `build`: 빌드 도구, 패키지, 번들 설정 변경
- `ci`: GitHub Actions 등 CI/CD 설정 변경
- `revert`: 이전 변경 되돌리기

### 좋은 예시

```text
feat: 1·2스테이지 근접 몹을 봉 휘두르기 공격으로 변경

- 접촉 데미지 대신 접근→들어올림→내려치기→후퇴 사이클로 공격
- 봉을 상태별 각도로 회전하고 넉백 경직 시 스윙 취소
- `StageConfig.meleeSwing`으로 1·2스테이지에만 적용
```

```text
fix: BLE 재전송 요청 후 누락 패킷이 복구되지 않는 문제 수정

- 수신 측 누락 시퀀스 계산 오류 수정
- 재전송 완료 후 진행률이 갱신되도록 처리
- 중복 수신된 패킷은 파일에 다시 기록하지 않도록 방지
```

```text
refactor: 적 공격 상태 전환 로직을 별도 상태 머신으로 분리

- 접근, 공격 예고, 공격, 후퇴 상태를 명시적으로 분리
- 중복된 타이머 초기화 코드를 공통 함수로 이동
- 기존 게임 플레이 동작은 유지
```

### 피해야 할 예시

```text
수정
```

```text
feat: 이것저것 변경
```

```text
feat: 공격 수정

- 코드 수정
- 버그 고침
```

위 메시지들은 변경 목적과 실제 내용을 알기 어려우므로 사용하지 않는다.

### 에이전트 작업 규칙

Codex 또는 Claude Code가 커밋을 생성할 때는 반드시 다음 순서를 따른다.

1. `git status`와 `git diff`로 변경 내용을 확인한다.
2. 변경의 주된 목적에 맞는 태그를 하나 선택한다.
3. 제목에 핵심 변경 사항을 구체적으로 작성한다.
4. 본문에 중요한 변경 사항을 `-` 목록으로 정리한다.
5. 서로 관련 없는 변경이 섞여 있으면 사용자에게 커밋 분리를 제안한다.
6. 사용자가 명시적으로 요청하지 않은 경우 임의로 커밋하지 않는다.
